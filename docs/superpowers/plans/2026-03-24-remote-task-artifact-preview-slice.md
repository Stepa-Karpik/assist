# Remote Task Artifact Preview Slice

1. Add renderer typings for task snapshot artifact metadata.
2. Add RED tests proving image artifacts render in `Чаты Telegram` and
   `Невыполненное`.
3. Introduce a shared artifact helper for remote task cards.
4. Render inline preview + file name in both pages.
5. Run desktop tests, typecheck, and package verification.
