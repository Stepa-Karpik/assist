# Capability Help Slice

1. Add failing bot tests for `/help` text and updated `/start` guidance.
2. Add failing renderer test for supported task examples in settings.
3. Implement help text helpers and register `/help` in the bot dispatcher.
4. Add a compact capability card to desktop settings.
5. Run bot tests, desktop tests, desktop typecheck, and desktop package.
6. Commit the slice separately.
