# Docs

This folder contains committed project documentation for `Karpik`.

Committed documentation includes:

- design specifications
- implementation plans
- contracts between components
- setup and deployment runbooks

Runtime user data is not stored here inside the repository, even if the
installed application later uses a `docs/user` layout in its own data root.
