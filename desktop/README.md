# Desktop

This folder contains the Windows desktop application for `Karpik`.

Responsibilities:

- Electron shell
- React renderer
- tray icon and quick popup
- local chats and blocked task UI
- local runtime bootstrap
- communication with the local `codex` runtime
- sync with the remote server

This folder produces the Windows installer and the installed desktop app.

