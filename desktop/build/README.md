# Desktop Build Assets

This directory stores packaging-facing assets for the Windows desktop app.

Planned contents:

- installer icons
- application icons
- splash or branding assets if packaging needs them later

The current bootstrap phase relies on Electron Forge metadata from
`forge.config.ts`. Real icon assets can be added here without changing the
source layout.
